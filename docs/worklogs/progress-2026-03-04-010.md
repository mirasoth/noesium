---
date: 2026-03-04
session: tui-output-optimization-impl
objective: Implement TUI Output Optimization Plan (layout order, subagent plan dynamic update, pinned block, SubagentTracker simplification, post-processing)
status: completed
---

# TUI Output Optimization Implementation

## Objective
Implement the merged TUI Output Optimization Plan: canonical layout order, subagent plan updates in real time, pinned global + live plan block, single streaming block without duplicate tracker lines, and no plan reprint after run.

## Completed
- 6.0 Layout order: docstring and numbered comments (1. Streaming, 2. Pinned, 3. Spinner) in `_build_display()`.
- 5. Subagent plan dynamic update: handle `step.start` / `step.complete` to update `subagent_plans[sid].steps[*].status`; on SUBAGENT_END mark remaining steps completed.
- 6.1 Pinned block: show only global plan + one “Live: [sid]” subagent plan when that subagent is active (`active_subagent_id`).
- 6.2 SubagentTracker: added `render_filtered(activity_lines, last_n)` and `render(exclude_sids)`; omit tracker lines for subagents that have recent activity lines.
- 6.3 Post-processing: removed re-print of plan trees; only partial results and final answer are printed after Live exits.

## Files Changed
- `noesium/noeagent/tui.py` – Layout docstring and comments; `active_subagent_id` and set/clear on SUBAGENT_*; step.start/step.complete/SUBAGENT_END handling for subagent plan; pinned block shows only global + active subagent plan; `SubagentTracker._sids_with_recent_activity`, `render_filtered`, `render(exclude_sids)`; post-processing no longer prints plan trees.

## Tests
- `tests/agents/test_noe_agent.py` and `tests/agents/test_noe_taskplan_and_tui.py`: all 61 tests passed.
- `tests/integration/test_noe_subagent_integration.py::TestEndToEndStreamingIntegration::test_tui_receives_subagent_events`: skipped (integration marker); no failure.

## Notes
- Plan file was not edited per user request. Reference: TUI Output Optimization Plan (merged layout order).
- `active_subagent_id` is set on SUBAGENT_START/SUBAGENT_PROGRESS and cleared on SUBAGENT_END so the pinned “Live” plan shows only the currently running subagent.

## Next Steps
- Optional: tune Tacitus progress summaries for consistency with main agent (plan 6.4). Manual TUI run to confirm layout and subagent plan updates live.

---
date: 2026-03-05
session: split-noeagent-framework-polish-006
objective: Continue RFC-1007 split by removing remaining framework-level NoeAgent coupling in public package surfaces
status: completed
---

## Objective
Continue separating NoeAgent from the Noesium framework package so `noesium` presents framework/subagent primitives without application-layer coupling.

## Completed
- Removed `noeagent` imports and exports from top-level `noesium/__init__.py`.
- Cleaned packaging metadata in `pyproject.toml` to drop the `noeagent` console script and removed `noeagent` package-data references.
- Updated README messaging and architecture diagram to describe framework-focused layers (`core`, `toolkits`, `subagents`) without `noeagent` as part of the main package story.
- Generalized `noesium/core/consts.py` doc comments to avoid explicit NoeAgent-specific examples.

## Files Changed
- `noesium/__init__.py` — removed NoeAgent-centric API exposure from top-level package.
- `pyproject.toml` — removed `noeagent` script/package-data references and benchmark marker wording update.
- `README.md` — revised positioning and architecture section to framework-only language.
- `noesium/core/consts.py` — generalized application home-dir comment examples.

## Tests
- `python -m py_compile noesium/__init__.py noesium/core/consts.py` ✅ pass
- `uv run python -c "import noesium; print(noesium.__version__)"` ⚠️ blocked by environment (`uv` parsing incompatibility + network fetch restrictions)

## Notes
- There are still many `noeagent` references in legacy docs/examples/tests and `coder-backend`; this pass focused on framework packaging and public API surface.
- `uv` in this environment reports unsupported `tool.uv.extra-build-variables` and cannot fetch packages due network tunnel errors.

## Next Steps
- Sweep and either migrate or archive legacy `noeagent`-specific docs/examples/tests to the standalone NoeAgent repository.
- Update backend integration (`coder-backend`) to depend on standalone NoeAgent package name/path.

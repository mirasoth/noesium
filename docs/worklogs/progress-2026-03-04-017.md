---
date: 2026-03-04
session: session-2026-03-04-017
objective: Centralize reusable toolkit and subagent registration names into core constants and apply across codebase
status: completed
---

## Objective
Centralize duplicated toolkit/subagent identifier strings into shared constants and refactor repeated usage sites.

## Completed
- Added a new core constants module for shared library identifiers (`noesium/core/library_consts.py`).
- Replaced hard-coded toolkit registration names in toolkit `@register_toolkit(...)` decorators with constants.
- Replaced hard-coded toolkit names used for temporary directory paths (`get_toolkit_tmp_dir(...)`) with constants.
- Updated NoeAgent config and command mapping code to reuse centralized toolkit/subagent constants.
- Updated built-in subagent factory mapping to use shared subagent constants.

## Files Changed
- `noesium/core/library_consts.py`: Added canonical toolkit and built-in subagent constant names.
- `noesium/core/toolify/mcp_integration.py`: Switched MCP toolkit registration to constant.
- `noesium/toolkits/*.py` (all toolkit modules): Switched toolkit registration names to constants; updated tmp-dir helper name arguments where applicable.
- `noesium/noeagent/config.py`: Replaced repeated toolkit/subagent strings in defaults with constants.
- `noesium/noeagent/commands.py`: Replaced repeated toolkit/subagent map keys with constants.
- `noesium/noeagent/agent.py`: Replaced built-in subagent factory map keys with constants.

## Tests
- Pass: `uv run --no-project python -m compileall noesium/core/library_consts.py noesium/core/toolify/mcp_integration.py noesium/toolkits noesium/noeagent/config.py noesium/noeagent/commands.py noesium/noeagent/agent.py`
- Note: Command prints an environment warning about `pyproject.toml` uv field parsing, but compilation succeeded.

## Notes
- `uv run` in this environment emits a TOML parsing warning for a repository `tool.uv` key, but execution still works for `--no-project` commands.
- An unrelated untracked directory `coder-frontend/node_modules/` existed in the working tree and was not included in this change.

## Next Steps
- None.

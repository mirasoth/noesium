---
date: 2026-03-04
session: noeagent-prompts-optimize
objective: Optimize noeagent prompts per claude_code_prompts style; default builtin tools/subagents only; safety and tool policy
status: completed
---

# NoeAgent Prompts Optimization

## Objective
Optimize `noesium/noeagent/prompts` to align with `claude_code_prompts.md`: document only default built-in tools and subagents with usage based on current implementation; add safety and optimization guidance.

## Completed
- Updated `agent_system.md`: default built-in toolkits (8) and subagents (browser_use, tacitus) only; tool usage from registry via `{tool_descriptions}`; subagent usage `action=invoke_builtin`, `name`, `message`; safety (security, no URL guessing, no time estimates, objectivity, over-engineering avoidance); tool usage policy (parallel when independent, prefer specialized tools over bash).
- Updated `ask_system.md`: safety/objectivity note (no fabrication, no URL guessing).
- Updated `planning.md` and `revise_plan.md`: only default built-in toolkits and subagents; execution modes aligned with implementation; builtin_agent = browser_use or tacitus with invoke_builtin.
- Updated `reflection.md`: objectivity, no time estimates.
- Updated `finalize.md`: quality/safety (accuracy, cite sources, no fabrication, no time estimates).
- Updated `README.md`: capabilities section describes default built-in only and runtime-injected tool_descriptions; added Safety and Optimization section.

## Files Changed
- `noesium/noeagent/prompts/agent_system.md` - Full rewrite: default tools/subagents, safety, tone, tool policy
- `noesium/noeagent/prompts/ask_system.md` - Safety and objectivity
- `noesium/noeagent/prompts/planning.md` - Default builtin only, execution modes table
- `noesium/noeagent/prompts/revise_plan.md` - Default builtin only
- `noesium/noeagent/prompts/reflection.md` - Objectivity, no time estimates
- `noesium/noeagent/prompts/finalize.md` - Quality/safety, sources, no fabrication
- `noesium/noeagent/prompts/README.md` - Capabilities and Safety and Optimization sections

## Tests
- No code changes; prompt template edits only. No test runs required.

## Notes
- Tool list is built at runtime by `_build_tool_descriptions(registry)` in nodes.py; prompts now state that and list default toolkits as reference only.
- Built-in subagent invocation is `SubagentAction(action="invoke_builtin", name="browser_use"|"tacitus", message=...)` per nodes.py and schemas.py.

## Next Steps
- None. Optional: run agent with new prompts in ASK/AGENT modes to validate behavior.

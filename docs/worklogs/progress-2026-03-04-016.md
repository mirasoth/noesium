---
date: 2026-03-04
session: docs-and-cleanup-2026-03-04
objective: Update all docs and remove unused code after explicit subagent selection and multi-subagent
status: completed
---

# Docs Update and Unused Code Cleanup

## Objective
Update documentation and remove unused code after switching to explicit subagent selection (no slash parsing) and multi-subagent support.

## Completed
- Removed slash-parsing code from `noesium/noeagent/commands.py`: `SUBAGENT_COMMANDS`, `_COMMAND_PATTERN`, `parse_inline_command`, `is_subagent_command`, `get_subagent_commands_help`; dropped `re` import.
- Updated `noesium/noeagent/__init__.py`: removed `parse_inline_command`, `is_subagent_command`, `get_subagent_commands_help`; added `BUILTIN_SUBAGENT_NAMES`, `inline_command_from_subagent`, `parse_subagent_prefix_from_input`, `validate_subagent_names`.
- Removed dead `_process_subagent_command` from `noesium/noeagent/tui.py` (subagent flow now goes through `_process_query` with `subagent_names`).
- Updated `noesium/noeagent/agent.py` docstring for `_handle_inline_command` (explicit subagent, not slash).
- Updated prompts: `noesium/noeagent/prompts/agent_system.md` (Tacitus explicit selection); `noesium/noeagent/nodes.py` (two message strings).
- Rewrote **docs/impl/noe-tools-and-subagent-integration.md** section 1 to "Explicit Subagent Selection" (API, TUI prefix, multi-subagent); updated section 2 and one note to use "explicit selection" instead of "/research command".
- Updated `noesium/noeagent/commands.py` module docstring and `tui.py` module docstring (subagent selector).

## Files Changed
- `noesium/noeagent/commands.py` - Removed slash parsing and help; updated docstring.
- `noesium/noeagent/__init__.py` - Export list updated.
- `noesium/noeagent/tui.py` - Removed `_process_subagent_command`; updated docstring.
- `noesium/noeagent/agent.py` - Docstring for `_handle_inline_command`.
- `noesium/noeagent/prompts/agent_system.md` - Two notes on Tacitus.
- `noesium/noeagent/nodes.py` - Two user-facing messages.
- `docs/impl/noe-tools-and-subagent-integration.md` - Section 1 rewritten; section 2 and one note updated.

## Tests
- No new tests run. Existing tests not executed in this session.

## Notes
- `execute_subagent_command` and `InlineCommand` remain in commands.py and are used by `_handle_inline_command` (via `inline_command_from_subagent`). No external callers of `parse_inline_command` or `get_subagent_commands_help` remain after __init__ and tui updates.

## Next Steps
- None.

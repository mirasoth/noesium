# OpenAI LLM Provider Configuration
# Copy this file to .env in your project root and fill in your actual API key
#
# OpenAI provides access to GPT models, embeddings, and vision capabilities
# through their official API. This is the default provider for Cogents.

# Set OpenAI as the default LLM provider
COGENTS_LLM_PROVIDER=openai

# OpenAI API key (required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI base URL (default: https://api.openai.com/v1)
# Use default for official OpenAI API, or set custom URL for compatible services
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI chat model (default: gpt-3.5-turbo)
# Available GPT models:
# - gpt-4o (latest GPT-4 Optimized, fast and capable)
# - gpt-4o-mini (smaller, faster, cost-effective)
# - gpt-4-turbo (GPT-4 Turbo, high capability)
# - gpt-4 (GPT-4, original high-capability model)
# - gpt-3.5-turbo (GPT-3.5 Turbo, fast and cost-effective)
# - gpt-3.5-turbo-16k (GPT-3.5 Turbo with 16k context)
# See https://platform.openai.com/docs/models for latest models and pricing
OPENAI_CHAT_MODEL=gpt-3.5-turbo

# OpenAI vision model (default: gpt-4-vision-preview)
# Vision-capable models:
# - gpt-4o (multimodal, supports images and text)
# - gpt-4o-mini (multimodal, supports images and text)
# - gpt-4-turbo (multimodal, supports images and text)
# - gpt-4-vision-preview (legacy vision model)
# Note: gpt-4o and gpt-4o-mini are recommended for vision tasks
OPENAI_VISION_MODEL=gpt-4o

# OpenAI embedding model (default: text-embedding-3-small)
# Available embedding models:
# - text-embedding-3-large (3072 dimensions, highest quality)
# - text-embedding-3-small (1536 dimensions, balanced)
# - text-embedding-ada-002 (1536 dimensions, legacy)
# See https://platform.openai.com/docs/guides/embeddings for details
OPENAI_EMBED_MODEL=text-embedding-3-small

# Embedding dimensions (default: 1536 for text-embedding-3-small)
# Adjust based on your embedding model:
# - text-embedding-3-large: 3072 (or can be reduced to 256, 512, 1024, 2048, 3072)
# - text-embedding-3-small: 1536 (or can be reduced to 512, 1536)
# - text-embedding-ada-002: 1536 (fixed)
COGENTS_EMBEDDING_DIMS=1536

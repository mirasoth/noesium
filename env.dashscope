# DashScope (Alibaba Cloud) LLM Provider Configuration
# Copy this file to .env in your project root and fill in your actual API key
#
# DashScope is Alibaba Cloud's AI service platform offering Qwen series models
# and other AI capabilities through an OpenAI-compatible API interface.

# Set OpenAI provider (DashScope uses OpenAI-compatible API)
COGENTS_LLM_PROVIDER=openai

# DashScope base URL (OpenAI-compatible endpoint)
# This is the compatibility mode endpoint that makes DashScope work with OpenAI SDK
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# DashScope API key (required)
# Get your API key from: https://dashscope.console.aliyun.com/
OPENAI_API_KEY=your_dashscope_api_key_here

# DashScope chat model (default: qwen-plus)
# Available Qwen models:
# - qwen-turbo (fastest, cost-effective)
# - qwen-plus (balanced performance and speed)
# - qwen-max (most capable, best quality)
# - qwen-max-longcontext (supports longer context)
# See https://help.aliyun.com/zh/dashscope/developer-reference/model-introduction for all models
OPENAI_CHAT_MODEL=qwen-plus

# DashScope vision model (default: qwen-vl-plus-2025-08-15)
# Vision-capable models:
# - qwen-vl-plus (multimodal vision understanding)
# - qwen-vl-max (most capable vision model)
# Note: Model names may include version dates (e.g., qwen-vl-plus-2025-08-15)
# Check DashScope console for latest available vision models
OPENAI_VISION_MODEL=qwen-vl-plus-2025-08-15

# DashScope embedding model (default: text-embedding-v4)
# Available embedding models:
# - text-embedding-v1 (1536 dimensions)
# - text-embedding-v2 (1536 dimensions)
# - text-embedding-v4 (1536 dimensions, latest)
# All DashScope embedding models support both Chinese and English
OPENAI_EMBED_MODEL=text-embedding-v4

# Embedding dimensions (default: 1536 for DashScope embedding models)
# DashScope embedding models typically use 1536 dimensions
COGENTS_EMBEDDING_DIMS=1536